{
  "name": "@amihuman/ais",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8001",
    "build": "echo 'Python build - no compilation needed'",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8001",
    "lint": "echo 'Python linting skipped'",
    "type-check": "echo 'Python type checking skipped'"
  },
  "dependencies": {},
  "devDependencies": {}
}
